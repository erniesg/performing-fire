# Generated by rucksack. Commands are names, not secrets.
version: 1
commands:
  install:
    command: "npm ci"
    timeout_seconds: 600
    required: true
  test:
    command: "npm run test"
    timeout_seconds: 900
    required: true
  vm-verify:
    command: "infra/vm/verify.sh"
    timeout_seconds: 120
    required: false
